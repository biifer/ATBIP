<%= form_for(@gateway) do |f| %>
  <% if @gateway.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gateway.errors.count, "error") %> prohibited this gateway from being saved:</h2>

      <ul>
      <% @gateway.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :gateway_id %><br />
    <%= f.text_field :gateway_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
